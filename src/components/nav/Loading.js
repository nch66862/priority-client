export const Loading = () => {
    return <main className="modal--parent">
        <section className="modal--content">
            <h4>please wait while my free server hosting site awakens...</h4>
        </section>
    </main>
}